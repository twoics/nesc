name: 'NESCS CI/CD'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - run: cd ..
      - run: cd ..
      - run: python manage.py test

